<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Produksi: hanya HTTPS. Sambungan plain HTTP diblokir secara eksplisit. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
