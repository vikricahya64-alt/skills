---
name: combo-live-web-exec
metadata:
  version: 3.0.0
  category: Web
description: >-
  Eksekusi langsung terhadap web: ambil halaman/API publik, ekstrak data, olah bersama
  file lokal, lalu sajikan hasil — semua terpasang pada loop agen nyata cabang operasional
  dari PRIME `Data & AI Evolution`; dapat dijalankan lewat perintah seperti `Ambil data
  dari API publik dan olah jadi hasil`, `Eksekusi langsung ke halaman web publik`; punya
  jalur eksekusi nyata via alur `liveWeb` di agen. Ini adalah kemampuan COMBO operasional
  pada tier ADVANCED-CAP dari arsitektur kemampuan v3. Gunakan ketika diminta Ambil data
  dari API publik dan olah jadi hasil, Eksekusi langsung ke halaman web publik. Hindari
  menggunakannya untuk pekerjaan di luar bidang Web.
---
# Live Web Execution 🌐

Eksekusi langsung terhadap web: ambil halaman/API publik, ekstrak data, olah bersama file lokal, lalu sajikan hasil — semua terpasang pada loop agen nyata.

| | |
|---|---|
| **Layer** | combo |
| **Group** | Web & Live |
| **Tier** | ADVANCED-CAP |
| **Family** | prime-data-ai |
| **Runnable** | Ya |
| **Recipe** | liveWeb |
| **Version** | 3.0.0 |
| **Category** | Web |

## Perintah

- `Ambil data dari API publik dan olah jadi hasil`
- `Eksekusi langsung ke halaman web publik`

## Hasil yang dapat dieksekusi

- Eksekusi langsung terhadap web: ambil halaman/API publik, ekstrak data, olah bersama file lokal, lalu sajikan hasil — semua terpasang pada loop agen nyata.
- Terbukti dapat dieksekusi nyata: membangun artefak konkret via alur `liveWeb` di agen.

## Skill / domain dasar

`exa-search`, `documentation-lookup`, `deep-research`, `fetch`, `everything-claude-code`, `mcp-server-patterns`

## Tag

`web`, `live`, `fetch`, `scrape`
