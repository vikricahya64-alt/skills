---
name: combo-os-environment
metadata:
  version: 3.0.0
  category: Environment
description: >-
  Lingkungan kerja yang portabel, aman, dan reproduksibel: manajemen env deklaratif
  (Flox/Nix), bash defensif produksi, shellcheck, BATS, secrets management, dan
  provisioning agen — satu kemampuan untuk menjalankan apa pun secara konsisten di
  lingkungan apa pun cabang operasional dari PRIME `Cloud & Infrastructure Evolution`;
  dapat dijalankan lewat perintah seperti `Buat lingkungan dev reproduksibel dengan flox`,
  `Susun bash script defensif + shellcheck untuk production`; punya jalur eksekusi nyata
  via alur `osFlow` di agen. Ini adalah kemampuan COMBO operasional pada tier ADVANCED-CAP
  dari arsitektur kemampuan v3. Gunakan ketika diminta Buat lingkungan dev reproduksibel
  dengan flox, Susun bash script defensif + shellcheck untuk production. Hindari
  menggunakannya untuk pekerjaan di luar bidang Environment.
---
# OS & Reproducible Environments 🐧

Lingkungan kerja yang portabel, aman, dan reproduksibel: manajemen env deklaratif (Flox/Nix), bash defensif produksi, shellcheck, BATS, secrets management, dan provisioning agen — satu kemampuan untuk menjalankan apa pun secara konsisten di lingkungan apa pun.

| | |
|---|---|
| **Layer** | combo |
| **Group** | Sistem & Performa |
| **Tier** | ADVANCED-CAP |
| **Family** | prime-cloud-platform |
| **Runnable** | Ya |
| **Recipe** | osFlow |
| **Version** | 3.0.0 |
| **Category** | Environment |

## Perintah

- `Buat lingkungan dev reproduksibel dengan flox`
- `Susun bash script defensif + shellcheck untuk production`

## Hasil yang dapat dieksekusi

- Lingkungan kerja yang portabel, aman, dan reproduksibel: manajemen env deklaratif (Flox/Nix), bash defensif produksi, shellcheck, BATS, secrets management, dan provisioning agen — satu kemampuan untuk menjalankan apa pun secara konsisten di lingkungan apa pun.
- Terbukti dapat dieksekusi nyata: membangun artefak konkret via alur `osFlow` di agen.

## Skill / domain dasar

`flox-environments`, `bash-defensive-patterns`, `shellcheck-configuration`, `bats-testing-patterns`, `netmiko-ssh-automation`, `autonomous-agent-harness`, `python-configuration`, `secrets-management`, `docker-patterns`, `spark-environment-setup`, `uv-package-manager`, `managing-python-dependencies`, `deployment-pipeline-design`

## Tag

`os`, `reproducible`, `nix`, `env`
