---
name: combo-sql-data-workflow
metadata:
  version: 3.0.0
  category: Data
description: >-
  Workflow data menyeluruh yang benar-benar berjalan: buat skema SQL → isi data (dari
  file/Csv) → query agregasi → verifikasi hasil → laporan. Semua langkah dieksekusi nyata,
  bukan sekadar dijelaskan cabang operasional dari PRIME `Data & AI Evolution`; dapat
  dijalankan lewat perintah seperti `Buat workflow SQL dari CSV ke laporan`, `Analisis
  agregasi data dengan SQL nyata`; punya jalur eksekusi nyata via alur `dataFlow` di agen.
  Ini adalah kemampuan COMBO operasional pada tier ADVANCED-CAP dari arsitektur kemampuan
  v3. Gunakan ketika diminta Buat workflow SQL dari CSV ke laporan, Analisis agregasi data
  dengan SQL nyata. Hindari menggunakannya untuk pekerjaan di luar bidang Data.
---
# SQL & Data Workflow (Multi-Step) 🗃️

Workflow data menyeluruh yang benar-benar berjalan: buat skema SQL → isi data (dari file/Csv) → query agregasi → verifikasi hasil → laporan. Semua langkah dieksekusi nyata, bukan sekadar dijelaskan.

| | |
|---|---|
| **Layer** | combo |
| **Group** | Data |
| **Tier** | ADVANCED-CAP |
| **Family** | prime-data-ai |
| **Runnable** | Ya |
| **Recipe** | dataFlow |
| **Version** | 3.0.0 |
| **Category** | Data |

## Perintah

- `Buat workflow SQL dari CSV ke laporan`
- `Analisis agregasi data dengan SQL nyata`

## Hasil yang dapat dieksekusi

- Workflow data menyeluruh yang benar-benar berjalan: buat skema SQL → isi data (dari file/Csv) → query agregasi → verifikasi hasil → laporan. Semua langkah dieksekusi nyata, bukan sekadar dijelaskan.
- Terbukti dapat dieksekusi nyata: membangun artefak konkret via alur `dataFlow` di agen.

## Skill / domain dasar

`bigquery-sql`, `sql-optimization-patterns`, `database-design`, `data-engineering`, `graphify`, `signed-audit-trails-recipe`

## Tag

`sql`, `data`, `workflow`
