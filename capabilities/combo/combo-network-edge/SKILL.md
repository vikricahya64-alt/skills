---
name: combo-network-edge
metadata:
  version: 3.0.0
  category: Network
description: >-
  Rekayasa jaringan end-to-end: dari konfigurasi BGP/VLAN/Firewall/WAF hingga service mesh
  (Istio/Linkerd), mTLS, DNS caching (Pi-hole), observability latency kritis, dan SSH
  automation — satu kemampuan terpadu untuk membangun, mengamankan, dan mengobservasi
  jaringan apa pun, edge maupun cloud cabang operasional dari PRIME `Cloud &
  Infrastructure Evolution`; dapat dijalankan lewat perintah seperti `Rancang reverse
  proxy nginx dengan load balancer`, `Konfigurasi service mesh Istio untuk edge GKE`;
  punya jalur eksekusi nyata via alur `networkFlow` di agen. Ini adalah kemampuan COMBO
  operasional pada tier ADVANCED-CAP dari arsitektur kemampuan v3. Gunakan ketika diminta
  Rancang reverse proxy nginx dengan load balancer, Konfigurasi service mesh Istio untuk
  edge GKE. Hindari menggunakannya untuk pekerjaan di luar bidang Network.
---
# Network & Edge Engineering 🌐

Rekayasa jaringan end-to-end: dari konfigurasi BGP/VLAN/Firewall/WAF hingga service mesh (Istio/Linkerd), mTLS, DNS caching (Pi-hole), observability latency kritis, dan SSH automation — satu kemampuan terpadu untuk membangun, mengamankan, dan mengobservasi jaringan apa pun, edge maupun cloud.

| | |
|---|---|
| **Layer** | combo |
| **Group** | Jaringan |
| **Tier** | ADVANCED-CAP |
| **Family** | prime-cloud-platform |
| **Runnable** | Ya |
| **Recipe** | networkFlow |
| **Version** | 3.0.0 |
| **Category** | Network |

## Perintah

- `Rancang reverse proxy nginx dengan load balancer`
- `Konfigurasi service mesh Istio untuk edge GKE`

## Hasil yang dapat dieksekusi

- Rekayasa jaringan end-to-end: dari konfigurasi BGP/VLAN/Firewall/WAF hingga service mesh (Istio/Linkerd), mTLS, DNS caching (Pi-hole), observability latency kritis, dan SSH automation — satu kemampuan terpadu untuk membangun, mengamankan, dan mengobservasi jaringan apa pun, edge maupun cloud.
- Terbukti dapat dieksekusi nyata: membangun artefak konkret via alur `networkFlow` di agen.

## Skill / domain dasar

`network-bgp-diagnostics`, `network-config-validation`, `network-interface-health`, `gke-networking`, `gke-service-networking`, `google-cloud-global-frontend-configuration`, `google-cloud-networking-observability`, `google-cloud-waf-security`, `homelab-network-readiness`, `homelab-network-setup`, `homelab-pihole-dns`, `homelab-vlan-segmentation`, `homelab-wireguard-vpn`, `latency-critical-systems`, `netmiko-ssh-automation`, `hybrid-cloud-networking`, `istio-traffic-management`, `linkerd-patterns`, `mtls-configuration`, `Network Engineer`, `protocol-reverse-engineering`, `docker-patterns`

## Tag

`network`, `edge`, `proxy`, `service-mesh`
